# Maestro device flows

Run `maestro test .maestro/smoke.yaml` against an installed development build and
a reachable API. Field/release jobs add poor-network, permission denial, GPS
drift, backgrounding, rental disappearance, reroute, and arrival flows; they are
not claimed by the emulator-free repository CI.
